# Todo

## Next actions

- Add a smoke check for `/admin` and `/citaj` to the release process so a static-site deployment cannot silently remove the customer platform again.
- Confirm with Tamara that admin login and sending access to a new buyer work after the 2026-08-23 rollback.
- Plan and test a separate Next.js major-version upgrade to resolve the three high-severity production dependency advisories reported by `npm audit --omit=dev`.
- Review the new blog section locally with the client-provided content.
- Confirm whether future blog posts should remain on the homepage or move to separate blog pages.
- Confirm whether the new "O meni" portrait crop is acceptable on mobile and desktop.
- Monitor production after the blog and portrait update.

## Backlog

- Consider replacing the non-functional email signup UI with a real capture flow.
- Consider moving weekly blog posts to MDX once the archive grows.
